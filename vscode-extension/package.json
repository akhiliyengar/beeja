{
  "name": "builder-bridge",
  "displayName": "builder bridge",
  "description": "Exposes vscode.lm (GitHub Copilot models) on localhost so the builder Python CLI can call them.",
  "version": "0.1.0",
  "publisher": "builder",
  "private": true,
  "license": "MIT",
  "repository": { "type": "git", "url": "https://github.com/akhiliyengar/beeja" },
  "engines": { "vscode": "^1.95.0" },
  "categories": ["Other"],
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "builder bridge",
      "properties": {
        "builderBridge.port": {
          "type": "number",
          "default": 21847,
          "description": "Local HTTP port the builder bridge listens on (127.0.0.1 only)."
        },
        "builderBridge.defaultFamily": {
          "type": "string",
          "default": "claude-sonnet-4.6",
          "description": "Default vscode.lm model family if the request doesn't specify one."
        }
      }
    },
    "commands": [
      {
        "command": "builderBridge.showStatus",
        "title": "builder bridge: Show status"
      },
      {
        "command": "builderBridge.restart",
        "title": "builder bridge: Restart"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package --no-yarn -o builder-bridge.vsix"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.95.0",
    "typescript": "^5.4.0"
  }
}
